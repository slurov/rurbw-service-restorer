# fix.ps1 хранится байт в байт: git не трогает кодировку и переводы строк.
# Иначе raw-ссылка отдавала бы не тот файл, который проверялся перед релизом,
# и SHA256 из README бы не совпал. Файл намеренно без BOM - почему, написано
# в начале fix.ps1.
*.ps1 -text

*.md text eol=lf
LICENSE text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
